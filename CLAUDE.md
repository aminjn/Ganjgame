# Ganjgame — قوانین پروژه

## سند اول (قانون پروژه)
«سند اول» دو بخش دارد و هر دو **قانون** این پروژه‌اند؛ هیچ تغییری در کد، اعداد، یا ظاهر نباید با آن‌ها ناسازگار باشد:

1. `docs/سند-اول/Ganjgame.docx` — «بازی گنج — منطق کامل و چک‌لیست قوانین» (نسخه‌ی نهایی، ۱۳ سپتامبر ۲۰۲۶). متن استخراج‌شده‌اش در `docs/سند-اول/01-منطق-بازی-و-چک‌لیست-قوانین.md` است.
2. `docs/سند-اول/02-هدف-بصری.md` — هدف بصری (low-poly استایلایز، مدل‌های CC0 طبیعت Quaternius، خورشید ~۳۵ درجه با سایه، دوربین ثابت ۵۵ درجه / fov 30 بدون چرخش yaw، پراکندگی تصادفی صحنه، بلوم/وینیت/گرید ملایم، اجرا روی گوشی میان‌رده).

قواعد کار:
- هر عددی که به بازیکن نشان داده می‌شود باید از وضعیت واقعی بازی خوانده شود؛ عدد ساختگی ممنوع.
- هیچ بازیکن مجازی (ربات) وجود ندارد.
- قوانین خانه‌ها (تدارکات، قدرت موجودات، نیروها، فرمول تلفات) ثابت‌اند و فقط چیدمان زمین بین نقشه‌ها فرق می‌کند.
- زمین با شیدر رویه‌ای نقاشی‌گونه (اصلاحیه‌ی ۴ هدف بصری)؛ yaw دوربین هرگز نمی‌چرخد.
- اعداد پایه در `src/rules/constants.ts` هستند و باید با سند اول یکی بمانند؛ تغییر هر عدد فقط از پنل ادمین (در زمان اجرا) مجاز است.
- بخش «چیزهایی که عمداً در بازی نیست» در سند اول را دوباره اضافه نکنید.

## ساختار
- `src/rules/` منطق خالص و قابل تست (vitest) — بدون وابستگی به DOM/Three.
- `src/render/` رندر Three.js.
- `src/ui/` رابط فارسی راست‌به‌چپ.
- `src/net/` کلاینت شبکه (REST + وب‌سوکت). کلاینت هیچ منطق بازی اجرا نمی‌کند؛ فقط نمایش می‌دهد و کنش‌ها را به سرور می‌فرستد.
- `server/` سرور چندنفره‌ی مرجع (Fastify + SQLite داخلی Node 22): `src/game/` (دنیا، کنش‌ها، تیک)، `src/api/` (مسیرها، ادمین، وب‌سوکت)، `test/` (vitest). قوانین را از همان `src/rules/` می‌خواند.
- `deploy/` نصب روی سرور خالی اوبونتو (install.sh، systemd، nginx، certbot، پشتیبان) — راهنما در `deploy/README.md`.
- `admin.html` پنل ادمین (هیچ لینکی از بازی به آن نیست)، `faq.html` سوالات پرتکرار.
- `public/assets/models/` مدل‌های Quaternius (CC0). با `npm run assets` از پوشه‌ی منبع ساخته می‌شوند.

## دستورها
`npm run dev` · `npm run build` · `npm test` · `npm run assets` · سرور: `cd server && npm run dev|build|start|test`
